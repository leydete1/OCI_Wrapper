<?xml version="1.0" encoding="UTF-8"?>

<!--
========================================================================
 OCI_Test_Create_Session.xml

 Session Manager - Create Session stub request.

 This is NOT an <Insert_Template> - the Session Manager module builds
 that internally from the fields below.  This envelope stands in for
 the HTTP request payload that will replace it once the HTTP input
 module is in place.  Every field here is a stub value for local
 testing; a real client integration would populate client_id /
 client_ip / client_host from the actual caller.

 operation must be CREATE_SESSION for the dispatcher to route here.
 requested_ttl_seconds is optional - omit it (or set to 0) to use
 session_default_ttl_seconds from config.ini.
========================================================================
-->

<Session_Request>
    <operation>CREATE_SESSION</operation>

    <client_id>stub_client_001</client_id>
    <client_ip>127.0.0.1</client_ip>
    <client_host>localhost</client_host>
    <application_name>Data_Manager_Test_Runner</application_name>

    <requested_ttl_seconds>1800</requested_ttl_seconds>
</Session_Request>
